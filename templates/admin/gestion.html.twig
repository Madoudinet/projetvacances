{% extends 'base.html.twig' %}

{% block title %}Gestion des vehicules{% endblock %}

{% block body %}
<h1 class="text-center">Gestion des vehicules</h1>

<div class="container">
<table class="table table-striped">
    <thead>
        <tr>
            <th>id</th>
            <th>Titre</th>
            <th>Marque</th>
            <th>Modele</th>
            <th>Description</th>
            <th>Photo</th>
            <th>Prix</th>
            <th>Date d'enregistrement</th>
            <th>Options</th>
        </tr>
    </thead>
    <tfoot>
        <tr>
            <th>id</th>
            <th>Titre</th>
            <th>Marque</th>
            <th>Modele</th>
            <th>Description</th>
            <th>Photo</th>
            <th>Prix</th>
            <th>Date d'enregistrement</th>
            <th>Options</th>
        </tr>
    </tfoot>
    <tbody>
    {% for vehicule in vehicules %}
        
    <tr>
            <td>{{ vehicule.id }}</td>
            <td>{{ vehicule.titre }}</td>
            <td>{{ vehicule.marque }}</td>
            <td>{{ vehicule.modele }}</td>
            <td>{{ vehicule.description }}</td>
            <td>{{ vehicule.photo }}</td>
            <td>{{ vehicule.prix }} €</td>
            <td>{{ vehicule.dateEnregistrement|date('d/m/Y') }}</td>
            <td>
                <a href=""><i class="bi bi-eye-fill me-2"></i></a>
                <a href="{{ path('app_modify', {id: vehicule.id}) }}"><i class="bi bi-pencil-square me-2"></i></a>
                <a href="{{ path('admin_delete', {id: vehicule.id}) }}"><i class="bi bi-trash3-fill"></i></a>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>
</div>



{% endblock %}
